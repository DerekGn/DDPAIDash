<Page
    x:Class="DDPAIDash.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DDPAIDash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Page.BottomAppBar>
        <CommandBar x:Name="MainCommandBar" ClosedDisplayMode="Minimal">
            <AppBarButton x:Name="btnCamera" Icon="Camera" Click="btnCamera_Click" IsEnabled="DeviceModel.DeviceInstance.State == DeviceState.Disconnected"/>
            <AppBarButton x:Name="btnSettings" Icon="Setting" Click="btnSettings_Click" IsEnabled="DeviceModel.DeviceInstance.State == DeviceState.Connected"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid>
    </Grid>
</Page>
